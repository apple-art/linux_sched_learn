### 调度策略

线程的一个标签，刚被创建出来的时候，大家的策略都是SCHED_NORMAL。会有程序改变标签，看它是RT还是NORMAL。内核之后把它送到各自对应的调度类中。
FIFO/RR → rt，NORMAL/BATCH/IDLE → fair，DEADLINE → deadline

### 调度类

一套算法。框架问到这一类时，由它在自己的队列里挑下一个任务。
更高类还有人，这一类不会被问到。

### 调度器

按照下面的顺序，从不同类型里面，选择下一个要执行的进程，从而执行。
stop → deadline → rt → fair → idle
如果高级的类找到了线程执行，就不会问下一个类。



### 优先级

下面是对于线程优先级的安排。

| 谁                | 内核 `prio` | 越小越高                      |
| ----------------- | ----------- | ----------------------------- |
| RT                | 0–99        | 0 最高                        |
| 普通（NORMAL 等） | 100–139     | nice -20 → 100，nice 19 → 139 |

调度类里面会依据优先级给线程排序，决定谁最先被执行。

### CPU的运行队列

每个时刻，单个CPU核有着自己的一个运行队列，上面排满了等待这个核来运行的线程。

比如ABCDE五个线程，ABD在核0，CE在核1。



### 调度器入口



入口函数是`schedule()`，定义`kernel/sched/core.c`。

```
asmlinkage __visible void __sched schedule(void)
{
    struct task_struct *tsk = current;

    sched_submit_work(tsk);
    do {
        preempt_disable();
        __schedule(SM_NONE);
        sched_preempt_enable_no_resched();
    } while (need_resched());
    sched_update_worker(tsk);
}
```

真正干活的是__schedule，它会选择出下一个任务，放到next上。

| prev           | 当前正在运行的任务     |
| -------------- | ---------------------- |
| rq             | 当前 CPU 的运行队列    |
| next           | 调度器选出的下一个任务 |
| context_switch | 真正切换任务           |

### 何时调用调度器

主要有下面三个常见的场景，会进入调度器。

| 场景      | 谁主动发起    | 当前任务状态       | 是否一定马上切换                    |
| --------- | ------------- | ------------------ | ----------------------------------- |
| 时钟 tick | 时钟中断      | 通常仍在运行       | 不一定，只是可能设置 `need_resched` |
| 主动睡眠  | 当前任务自己  | 变成睡眠           | 会离开 CPU                          |
| 唤醒任务  | 其他事件/任务 | 被唤醒，重新可运行 | 不一定，要看它是否更值得运行        |





### 对于正常线程的CFS

理想的两个线程AB，希望他们在长时间的运行中，分别占用CPU算例的50%。

但是因为一些问题，AB会先后执行，有的因为一些事情，少执行了，为了实现公平，就需要一个虚拟参数来决定，下一轮调度点谁被执行，引入vruntime虚拟运行时间，如果一个线程获得的CPU多了，那么它的vruntime就大，反之则小，越是vruntime小的cpu，越需要被执行。

但是线程AB往往不是完全相等的，有优先级的说法，紧急的、快速做出响应的任务优先级高。因此让优先级高的线程，我们分配给它一个大权重，同样的cpu运行时间，vruntime变大的慢，从而获得更高的优先级。



### 对于正常线程的EEVDF

在理想调度CFS的基础上，linux6.6引入了EEVDF机制。它是为了考虑这样一个情况，线程A、B各自运行时间不一样，A下一轮运行1ms，B运行10ms，这样的话，明显应该先让A运行。

这样就引入一个新的参数slice，系统依据当前有多少个线程、各自线程的权重、系统希望多快进行切换等因素，综合考量，给各自线程一个slice。

> 这个slice并不是线程A、B本身要运行的时间，是系统依据当前状态，动态算出来的。比较玄乎，暂时不细究。

系统结合各自线程的vruntime+slice，算出一个统一的参数virtual deadline虚拟截止时间。接下来系统就依据虚拟截止时间，对线程进行排序了。这样的排序逻辑。

1. 引入一个“公平平均位置”，如果线程的vruntime超过这个值，说明已经占用CPU很久了，没有资格参与调度。
2. 对于有资格的线程，按照虚拟截止时间进行排序，选择最靠前的。



这样后，各自线程达到这样一个均衡的效果：

1. 线程A运行时间久了，vruntime就会变大，让出CPU
2. A的优先级高，vruntime上升的慢，可以多占用CPU或者更快抢到CPU
3. A的占用时间短，slice小，虚拟截止时间小，更容易抢到CPU。
